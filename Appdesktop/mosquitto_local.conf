# Mosquitto Configuration - Cho phép kết nối từ LAN
# Copy file này vào C:\Program Files\mosquitto\ và restart service

# Lắng nghe trên tất cả các IP
listener 1883 0.0.0.0

# Cho phép kết nối không cần xác thực (development)
allow_anonymous true

# Log
log_type all
